spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/news-test
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL82Dialect
        show_sql: true
